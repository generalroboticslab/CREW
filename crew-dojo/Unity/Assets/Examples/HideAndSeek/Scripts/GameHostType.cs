namespace Examples.HideAndSeek
{
    public enum GameHostType
    {
        // gather users into a waiting room and play
        WaitingRoom = 0,
        // join or leave at any time
        JoinLeave = 1,
    }
}
